{
  "name": "topluxe",
  "private": true,
  "version": "0.1.0",
  "description": "TopLuxe — marketplace premium sur Pi Network (monorepo racine, monolithe modulaire)",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=apps/web",
    "build": "npm run build --workspace=apps/web",
    "start": "npm run start --workspace=apps/web",
    "lint": "npm run lint --workspace=apps/web",
    "typecheck": "npm run typecheck --workspace=apps/web",
    "test": "npm run test --workspace=apps/web",
    "test:e2e": "npm run test:e2e --workspace=apps/web",
    "prisma:generate": "npm run prisma:generate --workspace=apps/web",
    "prisma:migrate": "npm run prisma:migrate --workspace=apps/web"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
