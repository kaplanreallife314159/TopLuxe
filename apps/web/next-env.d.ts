/// <reference types="next" />
/// <reference types="next/image-types/global" />

// NOTE: ce fichier est normalement généré/maintenu automatiquement par `next dev` / `next build`.
// Il est créé manuellement ici car ces commandes n'ont pas pu être exécutées dans cet
// environnement (absence d'accès réseau pour installer les dépendances — voir rapport TLX-001).
// Ne pas éditer son contenu à la main au-delà de cette étape de fondation.
